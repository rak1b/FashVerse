import React from 'react'

const Button = ({text}) => {
    return (
        <button className="bg-gray-400 w-auto py-2 rounded-lg text-gray-200 font-bold hover:bg-pyBlue-500">{text}
        </button>
    )
}

export default Button
